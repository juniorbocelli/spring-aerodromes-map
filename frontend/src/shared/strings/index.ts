import { ErrorMessages as _errorMessages } from './error-mesages';
import { Page as _page } from './page';

// ----------------------------------------------------------------------

const Strings = {
  ErrorMessages: _errorMessages,
  Page: _page,
};

export default Strings;